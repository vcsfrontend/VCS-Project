# name: Azure Static Web Apps CI/CD

# on:
#   push:
#     branches:
#       - stage
#   pull_request:
#     types: [opened, synchronize, reopened, closed]
#     branches:
#       - stage

# jobs:
#   build_and_deploy_job:
#     if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
#     runs-on: ubuntu-latest
#     name: Build and Deploy Job
#     steps:
#       - uses: actions/checkout@v3
#         with:
#           submodules: true
#           lfs: false
#       - name: Build And Deploy
#         id: builddeploy
#         uses: Azure/static-web-apps-deploy@v1
#         with:
#           azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_GRAY_WATER_0143F2900 }}
#           repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
#           action: "upload"
#           ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
#           # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
#           app_location: "/" # App source code path
#           api_location: "" # Api source code path - optional
#           output_location: "dist/browser" # Built app content directory - optional
#           ###### End of Repository/Build Configurations ######

#   close_pull_request_job:
#     if: github.event_name == 'pull_request' && github.event.action == 'closed'
#     runs-on: ubuntu-latest
#     name: Close Pull Request Job
#     steps:
#       - name: Close Pull Request
#         id: closepullrequest
#         uses: Azure/static-web-apps-deploy@v1
#         with:
#           azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_GRAY_WATER_0143F2900 }}
#           action: "close"

name: Build and deploy Angular app to Azure Static Web App

on:
  push:
    branches:
      - stage
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'  # Use the appropriate Node.js version

      # - name: Install Angular CLI
      #   run: npm install -g @angular/cli

      - name: Install dependencies
        run: npm install -f  # Use the -f flag for npm install

      - name: Build Angular App
        run: npm run build --prod

      - name: List dist directory
        run: ls -R ./dist

      - name: Upload artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: angular-app
          path: ./dist/preview/browser  # Adjust this to match your output folder

  deploy:
    runs-on: ubuntu-latest
    needs: build_and_deploy
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: angular-app
      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_GRAY_WATER_0143F2900 }}  # Set this in your secrets
          name: vcs-frontend  # Replace with your Azure Static Web App name
          app_location: "/"  # Adjust this to the path of your built app
          output_location: "/dist/preview/browser/"
          deployment_action: "upload"

